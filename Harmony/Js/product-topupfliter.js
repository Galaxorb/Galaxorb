if($(".filterall").css("left") === "Opx"){
    $(".filterall").animate({ left: "1000px",

    })
   
    }
    else if($(".filterall").css("left") === "1000px"){
        $(".filterall"),animate({
        left: "Opx",
    })
}